@page "/fordel"
@using AspireApp.WebApp.Database
@using AspireApp.WebApp.Services
@inject HolmenkollenDbContext Db
@inject DistributeStagesService DistributeStagesService

Fordel Etapper
}
@code {
    // private async Task DistributeGroups()
    // {
    //     // 1. Hent kandidater fra databasen
    //     var candidates = await Db.Candidates.ToListAsync();

    //     // 2. Kall på Distribute-metoden og hent listen med Position
    //     distributedPositions = DistributeStagesService.Distribute(candidates);
    }
}